version: '3'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config
    environment:
      - NODE_ENV=production 